# QuickChat - MASQ app

1. first i create frontend part using 
    + npm create vite@latest
        - Project name: frontend.
        - Select a framework: React.
        - Select a varient: JavaScript.
        - Run in Terminal: npm install.
        - Run in Terminal: npm install react-router-dom.
        - Then Install TailwindCSS using vite.
        - Then delete - *App.css* file, Clear - *App.jsx* & *index.css* file.
        - Then change project name from *index.html*.
        - Run the project: *npm run dev*.

